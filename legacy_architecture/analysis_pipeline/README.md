# Legacy Analysis Pipeline

This folder contains archived audit code that was moved out of the top-level repo surface.

It is kept only for historical reference around:

- `packages/crash_tests/domain_shift_audit`
- `packages/crash_tests/spikeforest_audit`

Do not treat this directory as the active runtime or notebook API. The current working code lives in `src/qc_thesis/`, `notebooks/`, `data_pipeline/`, and `scripts/`.
