# Table Index

## Main Text

1. Dataset summary
2. Split summary
3. `fpos` benchmark ladder with metrics
4. `fmiss` benchmark ladder with metrics
5. `fpos` leave-one-family-out robustness summary
6. `fmiss` main ablation summary
7. `fpos` versus `fmiss` family asymmetry table
8. Reproducibility and environment summary

## Appendix

1. Full `fpos` ablation tables
2. Full `fmiss` ablation tables
3. Per-family summaries for all thesis-cited waves
4. Per-recording summaries for all thesis-cited waves
5. Supplementary model registry
